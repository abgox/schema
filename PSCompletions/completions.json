{
    "$schema": "http://json-schema.org/draft-07/schema",
    "type": "object",
    "required": [
        "list"
    ],
    "properties": {
        "list": {
            "description": "所有可以被添加的补全\nAll completions that can be added.",
            "type": "array",
            "items": {
                "type": "string",
                "description": "每一个可以被添加的补全\nEvery completion that can be added.",
                "minLength": 1
            },
            "minItems": 1
        }
    }
}
