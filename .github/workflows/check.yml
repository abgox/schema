name: Check

on:
  workflow_dispatch:
  schedule:
    - cron: '0 1 * * 1' # 每周星期一1点

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v3
      with:
        fetch-depth: 1

    - name: Check update
      run: |
        .\script\check-update.ps1